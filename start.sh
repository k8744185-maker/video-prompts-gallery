#!/bin/bash
echo "Starting Video Prompts Gallery (HTML/Flask Architecture)..."

# Ensure dependencies are installed (if needed)
# pip install Flask gspread oauth2client pytz python-dotenv

echo "Starting Flask Server..."
python3 main.py
